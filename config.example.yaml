projects:
  alpha:
    room_id: "!REAL_MATRIX_ROOM_ID:matrix.fishingteam.su"
    project_id: 1784454
    project_slug: denbay0-test
    # Optional per-project secret override:
    # webhook_secret: replace-with-project-secret
